/*Funcion void del proyecto final de programación*/

#include<stdio.h>
#include<stdlib.h>
#include<math.h>

void proyecto3(){ //Se la da inicio al programa, pero en lugar de usar la funcion main, utilizamos el nombre del primer archivo

//Declaración de variables
		
	float rx[],ry[],rz[],mp=0.1,
	int m,G=43000                                                                                                                                                                                                                                                                                                                                                                                                                                                      ,i,j;
	double x[n],y[n],z[n];


FILE *datos;
FILE *resultados;
//Se tomara cada planeta en cuestion en determinado tiempo, para que en base a la relación de la fuerza, velocidades y también. 

for (j=0;j<n;j++)
{
    for(i=0;i<n;i++)
	{
if 

	 //Hacemos las operaciones pertinentes para relación entre planetas con cada uno de los ejes
	fx[j]= ((G*m*m)) / pow(fabs(rx[i]-rx[i+1]),3) *fabs (rx[i]-rx[i+1]) +fx[j]; //trabajando con el eje x
	
	fy[j]= ((G*m*m)) / pow(fabs(ry[i]-ry[i+1]),3) *fabs (ry[i]-ry[i+1]) +fy[j]; //trabajando con el eje y

	fz[j]= ((G*m*m)) / pow(fabs(rz[i]-rz[i+1]),3) *fabs (rz[i]-rz[i+1]) +fz[j];  //trabajando con el eje z

	//Se imprimen los resultados en otro archivo de texto.
	fprintf(resultados,"%lf\t %lf\t %lf\t",x[i],y[i],z[i],h);


















	float pi=3.1416;
	float masas,d,e=1,fuerza,rho;//d es la distancia entre dos puntos y es un puntero ya que es un valor diferente para cada partícula
	float G=4*(pow(pi,2));


        FILE *datos;
        FILE *parametros;


//formula de fuerza, formula de distancia entre dos puntos, formula de Lennard-Jones

//e, rho entran desde el archivo de parametros
for(i=0;i<n;i++){
fuerzax[]=4*e((12*(pow(rho,12))/(pow(d[],13))-((6*(pow(rho,6)/(
pow(d[],7))*i
fuerzay[]=4*e((12*(pow(rho,12))/(pow(d[],13))-((6*(pow(rho,6)/(
pow(d[],7))*j
fuerzaz[]=4*e((12*(pow(rho,12))/(pow(d[],13))-((6*(pow(rho,6)/(
pow(d[],7))*k
}

